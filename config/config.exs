import Config

# ExZvec requires zvec built from source.
# Set these environment variables before compiling:
#
#   export ZVEC_DIR=/path/to/zvec
#   export ZVEC_BUILD_DIR=/path/to/zvec/build
#
# See README.md for build instructions.
