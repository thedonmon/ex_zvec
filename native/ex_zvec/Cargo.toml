[package]
name = "ex_zvec"
version = "0.2.0"
edition = "2021"
description = "Rustler NIF bridge to zvec-rs embedded vector database (pure Rust)"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37"
zvec-rs = { path = "../../../zvec-rs" }
serde_json = "1"
